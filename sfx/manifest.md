# SFX manifest — Mosaic Pieces

`manifest.txt` is canonical (file | event id | description | usage context).
`manifest.json` is the generator input for `tools/generate_sfx_from_manifests.py`
(MOSS-SoundEffect v2.0, 48 kHz mono Opus 96 kbps VBR, loudness-normalized, 100 steps).
